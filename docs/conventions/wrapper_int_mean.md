# This is a convention file for wrapper_int_mean

This file describes all conventions for Wrappers return values (ints), here we'll describe the mean of all Wrappers return values.

```yaml
0: smt failed
1: smt had success
2: conn is unavailable
3: malloc failed

```